# 小猪存钱罐 · 我的小账本 🐷

一个纯本地、无广告、蜜桃粉可爱风的记账网页应用。数据只存在你自己的手机里，不联网、不上传任何服务器。

## 文件清单

| 文件 | 作用 |
| --- | --- |
| `index.html` | 应用本体（双击/浏览器打开即可用） |
| `manifest.webmanifest` | PWA 清单（决定名称、图标、全屏模式） |
| `sw.js` | 离线缓存，安装后没网也能用 |
| `icon-180.png` | iPhone 主屏幕图标 |
| `icon-192.png` / `icon-512.png` | 安卓 / 其他平台图标 |

## 一、快速体验（先试试好不好用）

把 `index.html` 传到 iPhone 上（AirDrop、微信文件传输、iCloud 都行），用 Safari 打开，就能直接记账了。

> 注意：这种「直接打开文件」的方式只能当临时体验用。Safari 对本地文件的存储支持不稳定，换页面或重启后数据可能丢失。正式使用请走下面的「安装成 App」。

## 二、正式安装成 App（推荐）

要让它在主屏幕上有图标、全屏运行、数据稳稳存住，需要把这几个文件放到一个「网址」上。最省事的是免费静态托管，几分钟搞定：

**方式 A：GitHub Pages（免费，推荐）**
1. 注册 GitHub，新建一个仓库（比如叫 `huajian-zhang`）
2. 把这 5 个文件上传到仓库根目录
3. 仓库 → Settings → Pages → 选 `main` 分支 → Save
4. 等一分钟，会得到一个网址，形如 `https://你的用户名.github.io/huajian-zhang/`
5. iPhone Safari 打开这个网址 → 点分享按钮 → 「添加到主屏幕」→ 完成 🎉

**方式 B：Netlify（更简单，拖拽即用）**
1. 打开 netlify.com，注册登录
2. 把整个文件夹拖到首页的部署区域
3. 几秒后得到一个网址，同样「添加到主屏幕」

安装到主屏幕后，它就是全屏的原生 App 体验：有图标、没地址栏、没网也能打开。

## 三、数据安全说明

- 所有数据用浏览器 `localStorage` 存在你自己的设备上，**不上传任何服务器**，隐私天然有保障。
- 但正因如此，**换手机、清浏览器数据、卸载都会丢数据**。请在「我的 → 导出备份」定期备份，需要时用「导入备份」恢复。
- 备份文件是 `.json`，可以自己留档。

## 四、功能一览

- **今天**：本月结余、每日问候、月度预算进度条、今日流水（点记录可删除）
- **记一笔**：收支切换、自定义金额键盘、emoji 分类、账户选择、备注、日期
- **统计**：按月看分类占比甜甜圈图、分类排行，可切换月份
- **目标**：实时总存款看板（现金 + 别人欠你的钱）、月度消费阈值、自定义起止日期的年度存款目标、别人欠我（借出 / 还款自动联动）
- **我的**：账户管理（支付宝 / 微信 / 多张银行卡）、分类管理、数据导出导入、清空
- **预算提醒**：打开 App 时，若当月已花超过阈值的 80% 或 100%，会温柔地提醒你
