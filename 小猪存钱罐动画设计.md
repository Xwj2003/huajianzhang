# 🐷 小猪存钱罐动画 · 详细设计（v1）

> 主题：小猪存钱罐。路线：纯 Canvas 程序化绘制（零素材）。本文是开工前的最后设计稿。

---

## 一、小猪长什么样（形状拆分）

全部用 Canvas 画，不依赖图片。身体主体固定用「蜜桃粉」品牌色，让它永远是那只标志性的粉猪；背景、光效、粒子跟随主题色，保证暗夜黑下也不突兀。

| 部位 | 画法 | 说明 |
| --- | --- | --- |
| 身体 | 一个大圆角椭圆 | 圆润的猪身 |
| 耳朵 | 两个小三角/椭圆，立在头顶 | 可爱关键 |
| 鼻子（snout） | 椭圆 + 两个小鼻孔 | 猪的标志 |
| 眼睛 | 两个小黑点，开心时弯成弧线 `⌒` | 表情随状态变 |
| 腿 | 4 个短小的椭圆 | 站姿 |
| 投币口 | 背部一条细缝 | 金币从这里进去 |
| 尾巴 | 一条小卷曲线 | 细节加分 |

**「圆润度」随存款变化**：存款越多，身体椭圆越鼓、猪越「胖」——把总存款映射成一个 0–1 的缩放系数。

---

## 二、动画状态机（3 个状态 + 2 个过渡）

### 状态 1：空闲 Idle（常驻）
- 猪轻微「呼吸」（身体上下 1–2px 起伏）；
- 每隔几秒眨一下眼；
- 眼睛是正常的圆点 `◉◉`，偶尔左右看。

### 状态 2：投币 Coin-drop（记收入时触发，约 2 秒）
时间线分 4 步：

1. **金币出现**（0–0.3s）：一枚金币从投币口上方落下；
2. **掉入 + 下压**（0.3–0.7s）：金币滑进缝里，猪身被压得往下「Q 弹」一沉；
3. **弹回 + 开心**（0.7–1.3s）：猪身弹回原位并轻微弹跳，眼睛眯成弯线 `⌒⌒`，脸颊泛红，嘴角上扬；
4. **回落到空闲**（1.3–2.0s）：恢复呼吸，若连续投币可叠加小跳。

金额越大，金币越大、弹跳越高（有「多投」的快感）。

### 状态 3：满溢庆祝 Full（达成存款目标时触发，约 3 秒）
- 金币从投币口**喷出**，四散 + 受重力落下（粒子系统）；
- 撒出彩色纸屑/星星；
- 猪转一圈、耳朵扑扇；
- 结束回到更「胖」的空闲态（因为存款变多了）。

---

## 三、触发点（3 个入口）

| 触发 | 时机 | 效果 |
| --- | --- | --- |
| **记一笔收入成功** | 保存收入那一下 | 播放 2 秒投币动画（轻量，不打断操作，走 toast 弹层） |
| **首页顶部看板** | 常驻显示 | 小猪 + 当前总存款，点它可重播一次投币/满溢动画 |
| **达成年度目标** | 存款跨过目标线 | 播放 3 秒满溢庆祝 |

首发 MVP 建议先做前两个（投币动画 + 首页看板），第三个「庆祝」作为同一套粒子系统的自然延伸，一起带上。

---

## 四、放哪里

- **首页「今天」**：在问候语下方、本月结余卡片上方，加一张「🐷 存钱看板」小卡片：左边 Canvas 小猪，右边「总存款 ¥xx · 目标进度 xx%」。点卡片重播动画。
- **记一笔成功**：复用现有 `toast` 的位置，叠加一层半透明 Canvas 投币动画，2 秒自动消失。

---

## 五、数据绑定

| 数据 | 映射到 |
| --- | --- |
| 总存款（`calcDeposit()`） | 猪的圆润度（0–1 缩放） |
| 年度目标进度 | 看板上的进度条 + 是否触发「满溢」 |
| 本次收入金额 | 投币动画的金币大小/弹跳高度 |

---

## 六、性能与主题适配

- 用一个独立的 `<canvas>` + 单个 `requestAnimationFrame` 循环，**只在动画播放或看板可见时运行**，空闲时暂停，省电不卡；
- 粒子数量控制在 20–40 个，低端安卓机不掉帧；
- 猪身固定粉色，背景/光效/粒子用 `--primary`、`--glow` 等变量，随 7 个主题自动切换；
- 尊重系统的「减少动态效果」设置（可选，若后续想要）。

---

## 七、工作量

- 猪的绘制（各部位 + 圆润度）—— 主体；
- 投币动画时间线 —— 核心；
- 粒子/庆祝 —— 复用，较快；
- 首页看板卡片 + 触发接线 —— 收尾。

预计一次开发完成 MVP（猪 + 投币 + 看板 + 庆祝）。

---

## 八、开工前最后确认

以下 3 个点请确认，确认后我就开始写代码：

1. **猪的配色**：身体固定蜜桃粉（推荐，保持品牌辨识）还是跟随主题色？
2. **看板位置**：放首页顶部（问候语下方）可以吗？
3. **投币触发**：只在「记收入」时触发投币动画，记支出不触发（支出可播一个轻微「叹气/瘪一点」的小反馈，可选）——你要不要支出也来点反馈？

（如果你对上面几点没有特别想法，直接回一句「就按你说的来」即可，我按推荐默认值开工。）

---

## 九、实际实现（v5.0 已上线）

- ✅ 纯 Canvas 程序化绘制，零素材
- ✅ 投币动画（记收入触发，金币入罐）
- ✅ 支出叹气动画（吸气 → 变瘪冒气 → 发抖 → 恢复）
- ✅ 达成目标满溢庆祝（金币 + 彩带粒子）
- ✅ 点击小猪随机互动（跳跳 / 左右摇 / 转圈）
- ✅ 首页存钱看板卡片（存款进度 + 本月预算进度）
